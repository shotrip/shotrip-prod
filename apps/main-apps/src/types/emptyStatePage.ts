import { Locale } from "./params"

export type EmptyPrefectureStatePageProps = {
    prefectureLabel?: string,
    locale: Locale
}

export type EmptyCategoryStatePageProps = {
    categoryLabel?: string,
    locale: Locale
}